void main(List<String> args) {
  // For functions that contain just one expression, you can use a shorthand syntax:
}

class t {
  var _nobleGases = List<bool>.from([]);
  bool isNoble(int atomicNumber) => _nobleGases[atomicNumber] != null;
}
